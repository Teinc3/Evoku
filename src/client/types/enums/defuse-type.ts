/**
 * Enum representing the type of defuse objective for incoming attacks.
 * Row = horizontal line, Col = vertical line, Box = 3x3 grid.
 */
enum DefuseType {
  ROW = 'row',
  COL = 'col',
  BOX = 'box'
}

export default DefuseType;
