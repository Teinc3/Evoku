/**
 * Defines the possible application views for state-based navigation.
 * These correspond to the main screens in the application.
 */
export enum AppView {
  MAIN_MENU,
  BOARD_DEMO,
  NETWORK_DEMO
}

export default AppView;