# Development Environment
# For local development (ng serve, npm run dev)

# Angular Client Variables (injected into browser bundle)
NG_APP_WEBSOCKET_URL=ws://localhost:8745/ws

# Shared Variables (available to both client and server)
NG_APP_PACKET_SCRAMBLER_SEED=dev-scrambler-seed-123

# Server Variables (Node.js only)
BACKEND_PORT=8745
